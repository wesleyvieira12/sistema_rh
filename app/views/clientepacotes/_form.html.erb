<%= form_for(clientepacote) do |f| %>
  <% if clientepacote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clientepacote.errors.count, "error") %> prohibited this clientepacote from being saved:</h2>

      <ul>
      <% clientepacote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :client_id %>
    <%= f.text_field :client_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
