<%= form_for @tipo_pacote, :html => { :class => "form-horizontal tipo_pacote" } do |f| %>

  <% if @tipo_pacote.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@tipo_pacote.errors.count, "error") %> prohibited this tipo_pacote from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @tipo_pacote.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>

  <div class="form-group">
    <%= f.label :numeroEntrevistas, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :numeroEntrevistas, :class => 'form-control' %>
    </div>
    <%=f.error_span(:numeroEntrevistas) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit "Cadastrar", :class => 'btn btn-primary' %>  
    </div>

  </div>

<% end %>